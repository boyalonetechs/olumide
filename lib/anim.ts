/**
 * Shared motion easing — a buttery "expo-out" bezier used across
 * every entrance animation so the whole site feels consistent.
 */
export const EASE: [number, number, number, number] = [0.22, 1, 0.36, 1];